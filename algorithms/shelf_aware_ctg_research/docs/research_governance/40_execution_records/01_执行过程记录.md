# 执行过程记录

## 1. 记录模板

```text
批次：
阶段：
目标：
范围：
非范围：
输入：
输出：
实施结果：
验收结果：
结论：
遗留问题：
下一步：
提交：
```

## 2. 当前准备记录

### 2026-05-05 开工前治理文档准备

批次：开工前治理文档准备

阶段：B/C/D/E/F/G 正式研究前准备

目标：

- 按 `docs/refactor_governance` 格式，为 `shelf_aware_ctg_research` 建立研究治理文档。
- 固定自主推进边界、阶段计划、开工前检查、目录规划、验收入口和 Git 策略。
- 明确后续不恢复独立 axis path，不直接做组合算法。

范围：

- `algorithms/shelf_aware_ctg_research/docs/research_governance/`
- `algorithms/shelf_aware_ctg_research/README.md` 的文档索引。

非范围：

- 不改源码。
- 不改正式 planner。
- 不新增诊断脚本。
- 不重新跑 baseline。
- 不提交 output。

输入：

- 现有 `docs/refactor_governance` 目录格式。
- 当前 baseline-only 研究目录状态。
- 当前已确认的下一步顺序：A/B/C/D/E/F/G。

输出：

- 研究治理文档目录。
- 开工前准备清单。
- 目标目录与文件规划。
- 基线测试与验收入口。
- Git 提交与回滚策略。

实施结果：

- 已建立 `docs/research_governance/` 五类治理目录：background、architecture、execution_plan、preflight、execution_records。
- 已补齐开工前准备清单、目标目录与文件规划、基线测试与验收入口、Git 提交与回滚策略。
- 已在 README 中挂接研究治理目录。

验收结果：

- `find algorithms/shelf_aware_ctg_research/docs/research_governance -maxdepth 3 -type f` 已确认 13 个文档文件。
- `git diff --check -- algorithms/shelf_aware_ctg_research` 通过。
- 未执行源码测试，因为本批只新增文档和 README 索引。

结论：

- 后续可按该治理目录自主推进诊断与研究版模块优化。

遗留问题：

- 下一批应进入 B/C 加强诊断脚本，而不是算法优化。

下一步：

- 新增 residual grid node / jump target context 诊断脚本。

提交：

- 本批随提交 `补齐 shelf aware CTG 研究开工准备文档` 落地。


### 2026-05-05 B/C 残留网格节点与 jump target 诊断

批次：B/C 加强诊断脚本

阶段：B path + territory + jump 全局诊断；C grid node 质量诊断

目标：

- 新增 residual grid node / jump target context 诊断脚本。
- 对 baseline run 的 12 个 area 输出增强图和 JSON。
- 先解释节点质量与远跳原因，不改 planner。

范围：

- `algorithms/shelf_aware_ctg_research/scripts/diagnose_residual_grid_nodes.py`
- `algorithms/shelf_aware_ctg_research/docs/residual_grid_node_diagnostics_round1.md`
- `algorithms/shelf_aware_ctg_research/README.md`
- 本执行记录。

非范围：

- 不改正式 `shelf_aware_guarded`。
- 不恢复独立 axis path。
- 不提交 output run 图。
- 不实现 pruning。

输入：

```text
algorithms/shelf_aware_ctg_research/output/run_20260505_185541_861008
```

输出：

```text
<area>/diagnostics/residual_grid_nodes/
  01_path_territory_jumps.png
  02_nodes_debug_enriched.png
  03_jump_target_context.png
  grid_node_metrics.json
  jump_target_diagnostics.json
```

实施结果：

- 新增 `diagnose_residual_grid_nodes.py`。
- 对 12 个 area 生成 residual-grid-node 诊断。
- 输出 12 个 diagnostics 目录、36 张 PNG、24 个 JSON。
- 补充 `residual_grid_node_diagnostics_round1.md` 记录方法、限制和结论。

验收结果：

- `python3 -m compileall algorithms/shelf_aware_ctg_research/scripts/diagnose_residual_grid_nodes.py` 通过。
- `env PYTHONDONTWRITEBYTECODE=1 python3 -m algorithms.shelf_aware_ctg_research.scripts.diagnose_residual_grid_nodes --run-dir algorithms/shelf_aware_ctg_research/output/run_20260505_185541_861008` 通过。
- 图像非空检查通过：重点区域 9 张图均能读取且标准差非 0。
- 全量统计：`node_count=7318`，`jump_count=69`，`long_jump_count_ge_2m=50`，`jump_targets_within_overlap_radius=31`。

结论：

- overlap jump target 是真实存在的，但不能直接等价为可删除节点。
- `beiguo_lanshan_0407_area_6` 和 `fourfloor_area_7` 适合进入 D/E 判据研究。
- `fourfloor_area_6` 的长 jump 不能由 overlap 或边界敏感直接解释，需要保留为反例。

遗留问题：

- 当前 artifact 不能精确判断 `complete_cell_test()` 是否调整过中心，需要未来补 debug 字段。
- estimated degree 和 local free ratio 是诊断近似，不是 planner 内部真实网格指标。

下一步：

- 进入 D/E：生成 jump target 局部裁剪 context，并形成 optional/snap/insert 第一版判据。

提交：

- 本批随提交 `新增残留网格节点诊断` 落地。


### 2026-05-05 D/E residual candidate 规则分类

批次：D/E jump target context 与 residual candidate 判据

阶段：D 残留点与远跳原因分析；E optional/snap/insert 判据

目标：

- 读取 residual grid-node 诊断 JSON。
- 给每个 jump target 生成第一版 candidate 分类。
- 输出局部 context sheet 和规则 summary。

范围：

- `algorithms/shelf_aware_ctg_research/scripts/classify_residual_jump_targets.py`
- `algorithms/shelf_aware_ctg_research/docs/residual_candidate_rules_round1.md`
- `algorithms/shelf_aware_ctg_research/README.md`
- 本执行记录。

非范围：

- 不改 planner。
- 不删除节点。
- 不生成正式 after path。
- 不提交 output run。

输入：

```text
algorithms/shelf_aware_ctg_research/output/run_20260505_185541_861008
```

输出：

```text
<area>/diagnostics/residual_candidate_rules/residual_candidate_rules.json
<area>/diagnostics/residual_candidate_rules/01_jump_target_context_sheet.png
```

实施结果：

- 新增 `classify_residual_jump_targets.py`。
- 对 12 个 area 完成规则分类。
- 12 个 area 生成 `residual_candidate_rules.json`；8 个有 jump 的 area 生成 context sheet。

验收结果：

- `python3 -m compileall algorithms/shelf_aware_ctg_research/scripts/classify_residual_jump_targets.py` 通过。
- `env PYTHONDONTWRITEBYTECODE=1 python3 -m algorithms.shelf_aware_ctg_research.scripts.classify_residual_jump_targets --run-dir algorithms/shelf_aware_ctg_research/output/run_20260505_185541_861008` 通过。
- 重点区域 context sheet 非空检查通过。
- 全局分类：`optional_by_overlap_high_confidence=21`，`optional_by_overlap_needs_review=10`，`optional_by_near_overlap_needs_review=7`，`snap_or_insert_candidate=3`，`boundary_sensitive_review=5`，`required_or_topology_review=23`。

结论：

- overlap 两类候选共 31 个，适合进入第一版 pruning simulation。
- 23 个 required/topology review 说明不能做简单远跳删除。
- `fourfloor_area_6` 继续作为误删防护反例。

遗留问题：

- 当前 `avoidable_jump_length_upper_bound_m=132.932` 只是上界，不是实际路径收益。
- 还没有估算覆盖损失，也没有生成 before/after 伪路径。

下一步：

- 进入 F：只对 overlap 两类做研究版 pruning simulation，输出长度上界、覆盖风险和 before/after 诊断图。

提交：

- 本批随提交 `新增 residual jump target 分类` 落地。


### 2026-05-05 F overlap pruning simulation

批次：F 研究版 pruning simulation

阶段：F grid node optionalization / residual handling 研究版模拟

目标：

- 只对 overlap 两类 jump target 做后处理模拟。
- 估算简单删除 target point 的路径长度变化和局部覆盖风险。
- 不改 planner，不写回 baseline path。

范围：

- `algorithms/shelf_aware_ctg_research/scripts/simulate_residual_pruning.py`
- `algorithms/shelf_aware_ctg_research/docs/pruning_simulation_round1.md`
- `algorithms/shelf_aware_ctg_research/README.md`
- 本执行记录。

非范围：

- 不实现正式 pruning。
- 不做路径重连算法。
- 不改 `shelf_aware_guarded`。
- 不提交 output run。

输入：

```text
algorithms/shelf_aware_ctg_research/output/run_20260505_185541_861008
```

输出：

```text
<area>/diagnostics/pruning_simulation/pruning_simulation.json
<area>/diagnostics/pruning_simulation/01_overlap_pruning_simulation_overlay.png
```

实施结果：

- 新增 `simulate_residual_pruning.py`。
- 对 12 个 area 完成 overlap target pruning simulation。
- 每个 area 均生成 simulation JSON 和 overlay。

验收结果：

- `python3 -m compileall algorithms/shelf_aware_ctg_research/scripts/simulate_residual_pruning.py` 通过。
- `env PYTHONDONTWRITEBYTECODE=1 python3 -m algorithms.shelf_aware_ctg_research.scripts.simulate_residual_pruning --run-dir algorithms/shelf_aware_ctg_research/output/run_20260505_185541_861008` 通过。
- 输出检查：12 个 simulation 目录，12 张 overlay。
- 全局结果：`candidate_count=31`，`low_coverage_risk_candidate_count=23`，`simulated_length_delta_m=-19.077`。

结论：

- overlap 候选有价值，但简单删 target point 的收益远小于 jump 长度上界。
- 下一步应研究局部 reconnect / snap / insert，而不是直接进入正式 pruning。
- `fourfloor_area_6` 未产生 pruning candidate，反例保护有效。

遗留问题：

- 当前 coverage risk 是 target 局部 disk 估计，不是完整 swept-area 验证。
- 当前 simulated length delta 是后处理点删除估计，不证明机器人可执行。

下一步：

- 对低风险 overlap candidate 做局部 reconnect simulation；对 near-overlap / snap_or_insert candidate 做吸附点搜索。

提交：

- 本批随提交 `新增 overlap pruning simulation` 落地。


### 2026-05-06 F local reconnect / snap simulation

批次：F local reconnect / snap simulation

阶段：F grid node optionalization / residual handling 研究版模拟

目标：

- 对 overlap pruning candidate 评估前后路径直连是否 free。
- 对 near-overlap / snap candidate 评估是否能吸附到已有路径段。
- 继续保持不改 planner。

范围：

- `algorithms/shelf_aware_ctg_research/scripts/simulate_local_reconnect_snap.py`
- `algorithms/shelf_aware_ctg_research/docs/local_reconnect_snap_round1.md`
- `algorithms/shelf_aware_ctg_research/README.md`
- 本执行记录。

非范围：

- 不实现正式 fallback target optionalization。
- 不改 baseline path。
- 不提交 output run。

输入：

```text
algorithms/shelf_aware_ctg_research/output/run_20260505_185541_861008
```

输出：

```text
<area>/diagnostics/local_reconnect_snap/local_reconnect_snap.json
<area>/diagnostics/local_reconnect_snap/01_reconnect_snap_overlay.png
```

实施结果：

- 新增 `simulate_local_reconnect_snap.py`。
- 对 12 个 area 完成 reconnect / snap simulation。
- 输出 12 个 diagnostics 目录和 12 张 overlay。

验收结果：

- `python3 -m compileall algorithms/shelf_aware_ctg_research/scripts/simulate_local_reconnect_snap.py` 通过。
- `env PYTHONDONTWRITEBYTECODE=1 python3 -m algorithms.shelf_aware_ctg_research.scripts.simulate_local_reconnect_snap --run-dir algorithms/shelf_aware_ctg_research/output/run_20260505_185541_861008` 通过。
- 重点区域 overlay 非空检查通过。
- 全局 reconnect：`coverage_risk_review=3`，`reconnect_blocked=27`，`reconnect_ready=1`。
- 全局 snap：`snap_connector_blocked=8`，`snap_ready=1`，`snap_too_far=1`。

结论：

- 简单后处理删点并直连基本不可行，31 个 overlap candidate 中只有 1 个 reconnect_ready。
- 下一步应转向 fallback target 选择模拟，而不是继续做事后粗 pruning。

遗留问题：

- 当前 line free 检查基于 prepared free mask，不等于机器人完整动力学可执行性。
- snap 只找最近 prior segment，还没有搜索局部绕行路径。

下一步：

- 研究层模拟 fallback target optionalization：当 fallback target 已在实际清扫半径内且低覆盖风险时，跳过该 target，观察 jump 数和长 jump 数变化。

提交：

- 本批随提交 `新增 local reconnect snap simulation` 落地。


### 2026-05-06 F fallback optionalization feasibility

批次：F fallback optionalization feasibility

阶段：F grid node optionalization / residual handling 研究版可行性分析

目标：

- 审计现有 artifacts 是否足以重放 fallback target 选择。
- 给出 fallback optionalization 所需的缺失 debug 字段。
- 避免基于不完整 artifacts 伪造 planner replay 结论。

范围：

- `algorithms/shelf_aware_ctg_research/scripts/analyze_fallback_optionalization_feasibility.py`
- `algorithms/shelf_aware_ctg_research/docs/fallback_optionalization_feasibility_round1.md`
- `algorithms/shelf_aware_ctg_research/README.md`
- 本执行记录。

非范围：

- 不改正式 planner。
- 不补 debug artifact 字段。
- 不实现 fallback optionalization。

输入：

```text
algorithms/shelf_aware_ctg_research/output/run_20260505_185541_861008
```

输出：

```text
<area>/diagnostics/fallback_optionalization_feasibility/fallback_optionalization_feasibility.json
```

实施结果：

- 新增 `analyze_fallback_optionalization_feasibility.py`。
- 对 12 个 area 输出 fallback optionalization feasibility JSON。

验收结果：

- `python3 -m compileall algorithms/shelf_aware_ctg_research/scripts/analyze_fallback_optionalization_feasibility.py` 通过。
- `env PYTHONDONTWRITEBYTECODE=1 python3 -m algorithms.shelf_aware_ctg_research.scripts.analyze_fallback_optionalization_feasibility --run-dir algorithms/shelf_aware_ctg_research/output/run_20260505_185541_861008` 通过。
- 全局 proxy：`optional_overlap_candidate_count=31`，`optional_reconnect_ready_count=1`，`optional_reconnect_blocked_count=27`，`optional_coverage_review_count=3`。

结论：

- 当前 artifacts 不能精确重放 fallback target 选择。
- 后处理 pruning 主线应停止；真正有效的方向是 fallback 前 optional 判断或研究版 planner replay。
- 下一批应先设计/实现研究版 debug artifact 增强，而不是改正式算法。

遗留问题：

- 缺少 per-step fallback candidate list、candidate energy、unvisited set、stable node id、cell center adjustment 等字段。

下一步：

- 做研究版 debug artifact 增强方案；如要实施，应限制在研究目录或显式 debug-only 分支，避免污染正式 planner 行为。

提交：

- 本批随提交 `新增 fallback optionalization 可行性分析` 落地。


### 2026-05-06 F fallback debug artifact 与 trace optionalization

批次：F fallback debug artifact 与 trace optionalization

阶段：F grid node optionalization / residual handling 研究版证据增强

目标：

- 增强 `shelf_aware_guarded` debug artifact，补足 stable node id、grid row-col、center adjustment 和 fallback candidate energy。
- 用真实 `fallback_debug_trace.json` 对齐 optional overlap target。
- 验证 debug 增强不改变代表区域路径结果。

范围：

- `algorithms/coverage_planning/planners/shelf_aware_guarded/models.py`
- `algorithms/coverage_planning/planners/shelf_aware_guarded/grid_builder.py`
- `algorithms/coverage_planning/planners/shelf_aware_guarded/planner.py`
- `algorithms/shelf_aware_ctg_research/scripts/diagnose_path_grid_jumps.py`
- `algorithms/shelf_aware_ctg_research/scripts/diagnose_residual_grid_nodes.py`
- `algorithms/shelf_aware_ctg_research/scripts/analyze_fallback_trace_optionalization.py`
- `algorithms/shelf_aware_ctg_research/docs/fallback_trace_optionalization_round1.md`
- `algorithms/shelf_aware_ctg_research/README.md`
- 本执行记录。

非范围：

- 不改变 planner candidate selection。
- 不实现 optionalization。
- 不提交 output run。

输入：

```text
examples/maptools_projects/fourfloor
examples/maptools_projects/beiguo_lanshan_0407
```

输出：

```text
algorithms/shelf_aware_ctg_research/output/run_20260506_102054_774428
```

实施结果：

- 新增 `fallback_debug_trace.json` 和 `node_debug_enriched.json`。
- 扩展 `energy_debug.csv` 字段。
- 新增 `analyze_fallback_trace_optionalization.py`。
- 对新 run 重新跑完整诊断链。

验收结果：

- `python3 -m compileall` 对修改过的 planner/debug/research 脚本通过。
- 新 run 12 个 area 均成功。
- 每个 area 均产出 fallback trace、node debug JSON 和 energy CSV。
- 代表区域路径点数和路径长度与旧 run 一致。
- trace optionalization 全局结果：`fallback_event_count=221`，`optional_node_count=31`，`selected_optional_fallback_event_count=31`。

结论：

- optional overlap targets 全部是真实 global fallback 目标。
- 直接跳过这些目标会转向 energy 显著更高的候选；优化应转向 fallback 前 optional 语义或 grid node optionalization。

遗留问题：

- fallback trace 当前可能较大；后续如进入长期功能，需要考虑 debug 开关和体积控制。
- 尚未实现 optional replay 或正式算法改动。

下一步：

- 做研究版 optional node replay 设计，重点验证“optional 不进入必须访问集合”是否比 fallback 阶段跳过更合理。

提交：

- 本批随提交 `增强 fallback debug artifact` 落地。


### 2026-05-06 G territory node semantics research

批次：G territory node semantics research

目标：

- 回退 `6682f65` 中对正式 `shelf_aware_guarded` 的 optional/visited 行为改动。
- 删除 required-only optionalization 失败实验脚本和文档，避免后续误用。
- 落地新的节点语义研究方案：空间归属 -> 覆盖责任 -> 连通价值 -> 节点角色。

范围：

- `algorithms/shelf_aware_ctg_research/docs/territory_node_semantics_design.md`
- `algorithms/shelf_aware_ctg_research/src/node_semantics.py`
- `algorithms/shelf_aware_ctg_research/scripts/build_node_semantics.py`
- `algorithms/shelf_aware_ctg_research/README.md`

非范围：

- 不修改正式 planner 搜索行为。
- 不使用 axis map。
- 不删除/visited/snap/insert grid node。

初步验证：

```text
beiguo_lanshan_0407_area_1 node_count = 1249
node_role_counts = connector 335, cover_core 873, cover_soft 17, residual_soft 24
primary_space_counts = edge 1017, junction 212, unknown 20
```

输出示例：

```text
algorithms/shelf_aware_ctg_research/output/run_20260506_102054_774428/beiguo_lanshan_0407_area_1/diagnostics/node_semantics/
```
