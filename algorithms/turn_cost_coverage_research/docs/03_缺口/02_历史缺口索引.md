# 历史缺口索引

本文记录已经被拒绝、冻结或降级为参考的历史路线，防止后续重复走弯路。

## 1. 已拒绝迁入 UI 的路线

| 路线 | 结论 | 原因 | 当前位置 |
| --- | --- | --- | --- |
| official turn-cost 完整流程 | 不作为 UI 主线 | 依赖 official pipeline / solver / mesh 条件，且与当前 maptools 工作流不匹配 | `01_主题/02_shelfAwareTurnCost算法设计/06_演进边界与禁入路线.md` |
| square8 / axis-guided official flow | 不作为 UI 主线 | 某些场景形态改善，但覆盖率、图结构和后续连接不稳定 | `01_主题/02_shelfAwareTurnCost算法设计/01_算法定位与边界.md` |
| 全路径节点吸附 | 拒绝 | 改善部分过密，但制造过疏、左右不平衡和连接问题 | `01_主题/02_shelfAwareTurnCost算法设计/06_演进边界与禁入路线.md` |
| batch reconnect / shortcut | 不迁入正式链路 | 可局部改善，但缺少稳定 provenance、覆盖守卫和泛化证据 | `01_主题/02_shelfAwareTurnCost算法设计/05_路径实现与诊断产物.md` |
| lane family 简单平移 | 不迁入正式链路 | 容易按下一个问题又引入另一个问题，缺少拓扑级模型 | `03_缺口/01_当前缺口总表.md` |
| area5 专用规则 | 禁止 | 违反泛化原则，不能作为工程主线 | `01_主题/01_算法入口与能力边界.md` |

## 2. 已吸收的历史文档

已吸收历史文档不在 active docs 中保留原文。长期干货已经进入 `01_主题/`、`02_代码事实/`、`03_缺口/`、`05_验证/`、`reference/` 或仓库级 `docs/governance/`；需要追溯原始过程时使用 git 历史。

## 3. 后续使用规则

- 历史路线可以作为研究参考，但不能绕过 `01_主题/` 的算法边界。
- 如果要恢复某条路线，必须先补新的设计、数据结构、验收和回滚方案。
- 不能因为历史 output 中某张图效果好，就直接恢复旧脚本为 active 入口。
